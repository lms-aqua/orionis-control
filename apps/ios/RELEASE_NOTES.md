AdGuard query activity is now easier to scan, with explicit green Allowed and red Blocked results, live totals, loading feedback, and visible errors. This release also fixes the 0.1.8 build failure, prevents stale dashboard/event/DNS responses from replacing newer data, preserves the per-install device identity across sign-outs, and makes authentication cleanup more reliable.
